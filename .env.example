# nas-ports-view 部署配置示例
# 用法：cp .env.example .env  然后按需修改；docker compose 会自动读取 .env

# Web 监听端口（默认 8088）。host 网络模式下即宿主机监听端口。
PORT=8088

# 可选：手动指定宿主机 LAN IP（多网卡 / 自动探测不准时）
# HOST_IP=192.168.1.10

# 可选：Basic 认证，格式 user:pass
# BASIC_AUTH=admin:secret
